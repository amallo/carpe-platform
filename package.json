{
  "name": "carpe-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for CARPE Platform - Decentralized messaging system using LoRa and BLE",
  "workspaces": [
    "native"
  ],
  "scripts": {
    "native:install": "cd native && npm install",
    "native:install:ios": "cd native/ios && pod install && cd ../..",
    "native:install:all": "cd native && npm install && cd ios && pod install && cd ../..",
    "native:start": "cd native && npm start",
    "native:ios": "cd native && npm run ios",
    "native:android": "cd native && npm run android",
    "native:test": "cd native && npm test",
    "iot:build": "cd iot && pio run -e carpe-lora",
    "iot:upload": "cd iot && pio run -e carpe-lora --target upload",
    "iot:test": "cd iot && make test"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=9"
  }
}

